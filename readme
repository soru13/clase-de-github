Configuración global:
 Configurar git
git config --global user.name "Christian" 
git config --global user.email "xxxxxx@xxxx.com"
      
Próximos pasos:
  mkdir kiosko
  cd kiosko
  git init
  touch README
  git add README
  git-commit -m "primer commit"
  git remote add origin master https://github.com/soru13/kiosko. //conecto con github
  git push origin master//envio todo a github
  git pull origin master//me traigo todo de github

  //para clonar repositorios o lonarlos o bajarlos a mi pc
  git clone "url"//descargo todo o clono 